# API keys — never commit
server/config.js

# Dependencies
node_modules/

# Build output
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log

# Session data (in-memory only at runtime, but just in case)
*.session.json
